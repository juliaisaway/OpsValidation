{
  "name": "ops-validation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173 --strictPort",
    "sync:data": "node scripts/sync-data.mjs",
    "typecheck": "tsc --noEmit",
    "build": "npm run sync:data && npm run typecheck && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173 --strictPort"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "sass": "^1.77.2",
    "typescript": "^5.4.5",
    "vite": "^8.0.11"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
